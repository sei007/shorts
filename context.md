# Widforss Shorts — Context

Shoppable vertical-video feed prototype (TikTok-style) for Widforss (Nordic outdoor retailer). Single-file HTML prototypes, green brand palette. `PLAN.md` = production Sanity/Centra/Mux plan.

## Prototype flow (2026-07-02)
`start.html` (entry) → click a clip → `index-v2.html` (feed). This is the shareable prototype path.

## Files
- `start.html` — **entry/landing prototype** (2026-07-02). Blank white canvas, vertically centered, with the **"Klipp från er" module** + the Desktop/Mobile device-toggle (dark pill, ported from index-v2). Desktop module = responsive `.km-module` (max 1427, `dp-nav2` slider arrows drive an overflow scroll-snap track); mobile module (`.km-module-m`, 410-wide) shown via `body.mobile-mode`. **Card left edge aligns with the "Klipp från er" title** (rows use `padding-right` only — the earlier `margin:0 -36px` trick misaligned them). **6 cards, descriptive Swedish names (NOT people names, per Stefan):** Sommar vid sjön · Jakt med hund · Rast på fjället · Tre generationer · Natt i tältet · Ut på jakt. **Thumb behaviour (revised 2026-07-02):** every card is a **poster still that plays its clip on hover** (`.km-card:hover video`) — no autoplay, no "Spelas nu" badge (both removed; Stefan wanted a still frame, video only on hover). Mobile = static posters (no hover). Cards link to `index-v2.html` (mobile → `?view=mobile`; index-v2 reads the param and clicks the mobile toggle).
- `handoff.html` — **full dev-spec document** (rebuilt 2026-07-02 from scratch to clone `../Shop-in-Shop/handoff.html`'s pattern). Self-contained; all frames + specs assembled by one IIFE. **Pattern:** each screen has an **Inspect** frame (click any `.ix`/`data-k` element → live-measured spec in `.spec-panel`; orange `#E4572E` outline) + a **Spacing** frame (same markup, desaturated, redlines drawn from `getBoundingClientRect()` — `rel/mk/lbl/vdim/hdim/band` primitives, run after `document.fonts.ready`). **"Show all specs"** buttons dump every element's spec. Screens: 01/02 Entry module (desktop+mobile), 03/04 Shorts player (desktop+mobile), 05 single-product card, 06 "Shop the products" panel, 07 context menu + toast. Plus a **Motion** section (08 easing tokens w/ animated demo bars, 09 component animations). Values are **mirrored from `index-v2.html` (the source of truth)**; video shown as a still (dummy `generated/` image). SPEC data lives in `SPEC_A1…C3` objects; mock markup in `buildA1…buildC3`. (The old interactive entry-module blocks moved to `start.html`.)
  - **Refinements round 2 (2026-07-02):** new **play icon** (Stefan's 3-path `ic-play` glyph, in entry titles + start.html + icon grid). Section descriptions removed (fluff). **03/04 player = true 1:1** with index-v2 on a 1440×1024 viewport (video-h 774, content-area 957) so the **real ~91px top-nav→video gap is visible**; volume icon replaced with a proper `ic-sound` speaker. **Video progress bar + `2 tim sedan` timestamp are now click-inspectable** (own `.ix`/SPEC). **Inspect specs sit side-by-side, sticky right** (`.inspect-row`/`.col-r`) on player + overlay frames so you read spec while viewing the frame (no scroll-down). **03s/04s spacing show element-to-element vertical distances** (nav→title, nav→video, left inset, info→bottom) — the static top nav is NOT redlined (it's Shop-in-Shop's). **Overlays 05/06/07 render IN CONTEXT**: the real player behind, dimmed by a `.octx-scrim` (#000 @ .55), the highlighted overlay on top (inspect uses `octxCard/octxPanel/octxMenu`; spacing uses the isolated build). Toast removed from 07 (context menu only). **All icons in a downloadable grid** at the bottom (`buildIconGrid`/`downloadSymbol`, `ic-*` symbols). start.html: title→thumbnail gap is exactly **48px** (arrows absolute so head height == title height); cards align to the title's left edge. NOTE: `klipp-fran-er.mp4` is now slightly stale — Stefan added an outro (green card + logo + "8000 sommer-deals") to `shorts-video.html`; re-render if the first clip should include it.
  - **Doc-chrome redesign (2026-07-02, Fable):** Stefan: spec styling (not content) was unreadable — no system, bad contrast hierarchy. Whole document chrome now runs one system: type = 48 h1 / 24 h2 / 14 ui / 13 mono / 11 tracked-uppercase tags; text tones `--ink #141414`, `--body-c #5C5C56`, `--meta #8F8F8A`; hairlines `--line #E7E7E3`; panel bg `--panel #F6F6F3` (borderless, r12); **one button style** for everything (14/600, 1px #D8D8D3, r8 — olive frame-links killed; `.show-all.on` inverts to ink). Orange `#E4572E` reserved for redlines/inspect only (motion values + demo bars now ink). Frame labels restructured: mono number + 14/600 title + `INSPECT`/`SPACING` tag (`.label .n/.t/.tag`). `.cap` captions now legible 13px mono body-color. Removed unused `.stub` + duplicate `.inspect-row` CSS block. Spacing rhythm on 8pt: sections 160, frame-blocks 96 (48 after section head), labels mb 24. Mock screens untouched.
  - **Play-icon optical centering (2026-07-02):** the ▶ in «Klipp från er» titles was flex-centered on the line box = cap-height center, but sat ~3px high vs the word's full ink (descenders). Fix in start.html + handoff (both title sizes): `.km-title svg / .km-title-m svg { transform:translateY(0.07em) }`; documented as 'Icon offset' row in SPEC_A1/A2.
  - **Overlay focus + icon set (2026-07-02, round 2):** 05/06 inspect frames re-done per Stefan: no white header (playerDesktop takes a `noHeader` arg), `.octx` bg = `--bg`, and the old dark scrim replaced with `.octx-scrim.blur` — `backdrop-filter:blur(20px)` + rgba(31,30,29,.25), z-index 30 so it blurs ALL chrome (info column, rail, close); `.octx-fg` lifted to z 40. 07 (context menu) keeps the plain dark scrim. Icon grid now SKIPs ic-logo/search/user/cart/heart/share (still in the sprite for mocks) and gained **ic-muted** (speaker + ×, exact Stefan SVG from index-v2, viewBox 0 0 16 14). Deploy: push to GitHub `sei007/shorts` → Cloudflare Pages auto-builds `shorts-ec0.pages.dev`.
  - **Framing removed:** no `.canvas` box-shadow, no `.section-head` border-top (Stefan: no borders around content). **03 player is 1:1 with index-v2** (`.player.desktop` 1440 wide, video 574×1021). The **Widforss top nav is static** in 03 (not `.ix`; its spec lives in the Shop-in-Shop handoff — nav keys removed from `SPEC_B1`). Each inspect frame can carry a **link button + hint** (`s.link`/`s.note`): 01 → `start.html` ("plays on hover"), 03 → `index-v2.html`. **02s mobile spacing** shows vertical rhythm (above title / over+under thumbs / left inset), not the 260×324 dims (those are in the clickable spec). Spacing values are **rounded to even** in the mock geometry per Stefan (card 400, gap 48, thumb 90, sp-card 302, sp-thumb 50, toast min-width 148) — so the handoff mock diverges ~1px from index-v2's real odd values by design.
- `shorts-video.html` — standalone captioned slideshow clip (Ken Burns over `generated/*.png`, green per-line captions in Swedish, 4.2s/slide). Player aspect 574:1021. **Source used to RENDER `klipp-fran-er.mp4`** (see below); no longer iframe-embedded.
- `klipp-fran-er.mp4` — **the captioned "Klipp från er" montage rendered to a real mp4** (574×1020, 21s, h264, silent). Rendered 2026-07-02 by recording `shorts-video.html` with Playwright (temp `/tmp/vidrender`, chromium recordVideo → webm → ffmpeg h264 faststart). **This is now the FIRST slide of `index-v2.html`** as a normal `<video>` — the earlier iframe embed was unreliable (didn't show for Stefan), so it's baked to video and "treated like any other video" (progress bar, auto-advances on ended). It now carries its own left-column info like every other clip: `appendSlide({src:'klipp-fran-er.mp4', title:'Era bästa stunder', time:'Nyss', tags:['Friluft','Kund']})` (the `noInfo` flag was dropped — baked captions sit centre-bottom, the info column sits upper-left, no clash). **To re-render:** rerun `/tmp/vidrender/record.js` (Playwright) then the ffmpeg convert. The iframe branch in `appendSlide` is retained but unused.
- **Video edge (2026-07-02):** `.video-card::after` draws a 1px inset black ring (desktop only) to mask the light sub-pixel seam the rounded-corner mask left on the left edge of the video.
- `index.html` — original prototype (in-site, white header, right thumbnail rail, white shop drawer).
- `index-v2.html` — **current active variant.** Rebuilt 2026-06-21 to match the new Figma (`new[1896x1179].png` + annotation). See below.
- `index-v2-backup-*.html` — timestamped backups of prior v2 states (the `20260621-150319` one is the pre-Figma-redesign v2 with the right-side expanding panel, size chips, action rail, timestamped tagging).

## index-v2.html — current design (Figma "fullscreen player")
Fullscreen dark `#1F1E1D` shorts player (no site header). Font: **Bagoss Extended as a stand-in for "Greed Standard TRIAL"** (Greed not in /fonts — drop the .otf there and swap the @font-face family + `.si-title` etc.). Rule: never uppercase unless specified.

Layout per Figma:
- Close circle top-right: 40px `#292926`, white X.
- Video: centered, portrait aspect 574/1021, radius 12. Width derived from `--video-h`/`--video-w` CSS vars on `.content-area`; rail & left column positioned relative to it.
- **Site header (2026-07-02): refined Widforss nav ported from `../Shop-in-Shop/handoff.html`.** Main nav row only — dept menu (Herr active olive `#3D4B19` pill / Dame Barn Jakt Friluftsliv Hund), centered Widforss wordmark (inline `#ic-logo` SVG, 140×36, olive), right: search pill (405×38, r6, `--search-bg`, icon-left + input) + user icon + cart icon w/ green `#134B2B` badge. Deliberately **excludes** the olive promo/free-shipping banner, the mega-menu, and the sub-nav row (per Stefan). Icon symbol defs (`ic-logo/search/user/cart`) injected as a hidden `<svg>` right after `<body>`. Hidden on mobile.
- **Mobile nav (2026-07-02):** compact Widforss header (`.mobile-nav`) sticky at top on mobile, ported from handoff — burger left, logo (104×26) centered, cart+badge right, full-width search pill (46px, `#F3F3F3`) below. No promo banner. `padding: 26px 20px 16px` (top padding gives the logo breathing room). Sits above the player; video fills the content-area beneath it.
- **DESKTOP LEFT COLUMN IS UNCHANGED — do not touch.** `.shorts-info` stays left-anchored at `clamp(40px,4.3vw,81px)` / `top:33%`, aligned with the "Herr" top-nav item. `.vp-card-wrap` stays bottom-left. (A 2026-07-02 attempt to right-anchor these to the video was WRONG and reverted — the "void" complaint was about **mobile**, not desktop.)
- **Mobile text position (2026-07-02):** `.shorts-info` sits at `bottom:40px` (was 150px → floated mid-air). Single-product: `.shorts-feed.has-product` (toggled in `onSlideChange`) lifts it to `bottom:218px` = 40 + card 150 + 28px gap, so text sits **28px** above the card. Card-dismissed drops it back to 40px. The "Produkt fra video:" label (`.vp-card-label`) is **hidden on mobile** (`display:none`); it still shows + animates on desktop.
- **Shop panel mobile fix (2026-07-02):** the desktop `4.3vw` side padding was clipping cards on the 390px frame. Mobile now uses fixed 20px padding, full-bleed scroll row (`margin:0 -20px` + `padding:0 20px`), `scroll-snap-type:x`, and cards at `flex:0 0 74%` so card 1 aligns with the "Shop the products" heading and card 2 peeks for swipe affordance.
- Left column (`.shorts-info`, top:33% when no product): title 36px/115%, time 18px `#919191`, tag chips (64×24, r6, 1px white border, 14px). For **multi-product** videos a white "Shop all products" button (170×36, r12, 16px) sits below the chips.
- **Single-product** videos: bottom-left "Produkt fra video:" + white card 330×150 r12 (thumb 89×115 `#B8B8B8`, brand+name, "Gratis frakt!", price, green `#42541F` "Legg til", × to dismiss).
- Right rail: two `#3C3C39` arrow buttons (41×44 r12) + a `#292926` pill holding a **three-dots menu** (opens Lagre/Del — save+share hidden here, TikTok-style) and mute. **Mute button (2026-07-02) swaps two Stefan-supplied SVGs** — `.ic-sound` (speaker+waves) vs `.ic-muted` (speaker+×) — via `.muted` class on `#muteBtn` (starts muted).
- **Shop-panel scrim (2026-07-02):** `.shop-scrim` (z-index 56, between chrome/video at ≤55 and panel at 60) fades to 0.55 black on `.panel-open`, so the **black overlay now covers the video too**; click scrim closes the panel. Close-X (70) stays above.
- **Shop the products panel: horizontal, slides up from the bottom** (`.shop-panel`, translateY). Header "Shop the products" + chevron-down to collapse. Horizontal scroll row of `.sp-card` (thumb 64, name, gray price, green "Legg til"). Opening dims the left column + rail and nudges the video up.
- Toast feedback ("Lagt i handlekurv", "Lagret", "Lenke kopiert").
- Timestamped product tagging from the prior v2 was **removed** (kept lightweight per Stefan; production Mux timing TBD).
- Data: each video has `tags[]` and `products[]` (with `img`, `freight`). Products use new images `skoo.png` (boot), `caps.png` (cap), `harkilaa.png` (jacket); `mat.webp` for the waffle iron.
- Mobile mode (device toggle): basic adaptation only — desktop is the focus.

## Local server
`python3 -m http.server 8000` → http://localhost:8000/index-v2.html
Verify states with Chrome headless: append `#panel`, `#single`, `#menu` to a temp debug hook if needed (removed from shipped file). Use a separate `--user-data-dir`, `--virtual-time-budget≈2500`; long budgets hang.
